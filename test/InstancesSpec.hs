module InstancesSpec (spec) where

import Test.Hspec

spec :: Spec
spec = describe "SemiGroup for Lists" $ do
  it "close on list addition using length homomorphism" $ do
    pending
  it "validate associative laws with grouping" $ do
    pending
  it "validate associative laws without grouping" $ do
    pending